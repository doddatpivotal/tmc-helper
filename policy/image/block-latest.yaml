fullName:
  name: block-latest
spec:
  input: {}
  recipe: block-latest-tag
  recipeVersion: v1
  type: image-policy
  namespaceSelector:
    matchExpressions:
    - key: tmc-helper/tenant
      operator: Exists
