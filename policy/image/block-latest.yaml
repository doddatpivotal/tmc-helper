#@ load("@ytt:data", "data")
---
fullName:
  name: #@ data.values.name_prefix + 'block-latest'
spec:
  input: {}
  recipe: block-latest-tag
  recipeVersion: v1
  type: image-policy
  namespaceSelector:
    matchExpressions:
    - key: #@ data.values.label_namespace + "tenant"
      operator: Exists
